# Carbon Credits Frontend

This frontend is intentionally separate from CarbonCreditSystem.java.

Current stage:
- HTML/CSS/JS UI only.
- Java business logic is NOT modified.
- Demo data is used so the pages can be opened immediately.

Next integration stage:
Create an HTTP/API adapter around the existing Java logic. Do not rewrite the existing trading, emission, ledger, fraud or DSA logic.

Open index.html first.
